# Implementation Plan — Story 208115: Update site-wide phone number to +1 808 444-1168

**Type:** Verification-only. Acceptance criteria are already satisfied in code from a prior
content-fix pass; the one remaining gap is an uncommitted change. No new feature work required.

## Story

**As a** visitor to the Casa Colina Care website,
**I want** the contact phone number displayed correctly as +1 808 444-1168,
**so that** I can reach the facility at the correct number when calling to request a visit or
ask questions.

No parent Feature is linked to this story.

## Acceptance criteria (Gherkin) vs. current code

| # | Scenario | Touchpoint | Status |
|---|---|---|---|
| AC1 | Nav bar phone number is corrected | `app/page.tsx:24` | ✅ Met — staged, not yet committed |
| AC2 | Footer phone numbers match the corrected number | `app/page.tsx:341`, `:385` | ✅ Met — already committed in a prior session |
| AC3 | Phone number formatting stays consistent per location | nav `+1 (808) 444-1168` / footer `+1 808 444 1168` | ✅ Met |
| AC4 | No stale tel: links reference the old number | site has no `tel:` links currently | ✅ Met (nothing to verify beyond absence) |

## Outstanding work

1. **Commit the staged nav phone number change** — `app/page.tsx:24` is `git add`-staged from
   resolving an earlier merge conflict, but not yet committed.
2. **Browser verification** — confirm nav, footer div, and footer list item all render the
   corrected number after `bun run dev`.
3. **Close out** — transition story 208115 to Resolved/Closed once verified (currently left as
   Ready per the assignee's choice, pending manual confirmation).

## Child Tasks (already created in ADO)

| Task ID | Title | Effort (hrs) |
|---|---|---|
| 208183 | Commit staged nav phone number change | 0.25 |
| 208184 | AC1: Nav bar phone number is corrected | 0.25 |
| 208185 | AC2: Footer phone numbers match the corrected number | 0.25 |
| 208186 | AC3: Phone number formatting stays consistent per location | 0.25 |
| 208187 | AC4: No stale tel: links reference the old number | 0.25 |
| **Total** | | **1.25** |

Story fields: Story Points = 1, Iteration = `CasaColinaCare.com\Iteration 1`, State = Ready.
